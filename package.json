{
  "name": "pagina-grupo-hm",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio web de GRUPO H&M servido con Express para Railway",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required'",
    "setup": "npm install"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
